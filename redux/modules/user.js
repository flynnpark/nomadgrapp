// Imports

// Actions

// Action Creators

// API Actions

// Initial State

const initialState = {
    isLoggedIn: false
};

// Reducer

function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

// Reducer functions

// Exports

// Default Reducer Export

export default reducer;
